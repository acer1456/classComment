$('#myModal').html("<%= escape_javascript(render :partial => 'event') %>");

('#myModal').on('show.bs.modal', function () {
  $('#myModal').modal({
    focus: true,
    show: true
  })
 });

$('#myModal').on('hidden.bs.modal', function () {
   $('#myModal .modal-content').empty();
 });
