<!-- <script type="text/javascript">
('#myModal').modal('show.bs.modal', function () {
  $('#myModal').modal({
    focus: true,
    show: true
  })
  $('#myModal .modal-content').empty();
 });
</script> -->


<div class="row" >


<div class="col-sm-3 col-md-3">
  <div class="alert" style="padding:20px">
<%= form_tag(courses_path, :method => "get", id: "search-form") do %>
<p><%= text_field_tag :search, params[:search], placeholder: "課程 老師 類別",  class: "form-control"%></p>
<%= submit_tag "搜尋", class: "btn btn-block btn-info" %>
<% end %>
  <!-- <div class="">
  <a href="?utf8=✓&search=通識&commit=搜尋" class="btn btn-sm btn-success">通識</a>
  <a href="?utf8=✓&search=體育&commit=搜尋" class="btn btn-sm btn-success">體育</a>
  <a href="?utf8=✓&search=通識-社科&commit=搜尋" class="btn btn-sm btn-success">通識-社科</a>
  <a href="?utf8=✓&search=通識-自然&commit=搜尋" class="btn btn-sm btn-success">通識-自然</a>
  </div>
  <br>
  <a href="?utf8=✓&search=通識-文藝&commit=搜尋" class="btn btn-sm btn-success">通識-文藝</a> -->
  </div>
</div>


<div class="col-sm-9 col-md-9">
  <table class="table table-sm table-hover">
    <thead>
      <tr>
        <th>課程</th>
        <th id="hid">學期</th>
        <th id="hid">類別</th>
        <th>老師</th>
        <th>整體評價</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @courses.each do |course| %>
      <tr>
        <td><%= link_to course.name, course_path(course), :remote => true, data: {toggle: "modal", target: "#myModal"} %></td>
        <td id="hid"><%= course.semester %></td>
        <td id="hid"><%= course.category %></td>
        <td><%= course.teacher %></td>
        <td><% course.vote.times do %><i class="fa fa-star" style="color: #FDD835"></i><% end %></td>
        <!-- <td><%= link_to "詳細", course_path(course), :remote => true, data: {toggle: "modal", target: "#myModal"}, class:"btn btn-sm btn-info" %></td> -->
      </tr>
        <% end %>
    </tbody>
  </table>
</div>


</div>   <!-- row -->

<div class="modal" id="myModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true" focus="true">
</div>
